# set up apt
# Add Docker's official GPG key:
sudo apt-get update
sudo apt-get install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update


# install version
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin


##### TEST
sudo docker run hello-world

# Clonar repositorio

git clone https://github.com/RonaldChicche/robot_server.git


# entrar en carpeta de proyecto
cd robot_server


# Crear archivo .env

touch .env

# copiar contenido de env.txt a .env del proyeto y correcgir ip local con la ip nueva de la computadora
Vieja IP : 190.168.10.102
Nuebva ip: ???????????

# levantar proyecto 

docker compose build
docker compose up -d

# Verificar si estar los servicios levantados con 
docker ps

# Instalar portainer para vista de contenedores // se corre el comando como tal y ya
docker run -d -p 9443:9443 -p 9000:9000 -p 8000:8000 --name portainer --restart=always -v /var/run/docker.sock:/var/run/docker.sock -v portainer_data:/data portainer/portainer-ce:lts